import React from 'react';

type PaymentSummaryProps = {
    
};

const PaymentSummary = ({  }: PaymentSummaryProps): JSX.Element => {

    return (
        <section id='PaymentSummary'>

        </section>
    );
};

export default PaymentSummary;